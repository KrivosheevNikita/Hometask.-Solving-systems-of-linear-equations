#ifndef SEIDEL_H
#define SEIDEL_H
#include <vector>
#include <utility>

std::pair<std::vector<double>, int>  seidel(std::vector<std::vector<double>> a, std::vector<double> b, double e);

#endif

